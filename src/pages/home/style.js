import styled from "styled-components";

export const HomeWarpper = styled.div`
    position: relative;
    .am-flexbox {
        position: absolute;
        top: 25px;
        width: 100%;
        padding: 0 10px;
        z-index: 2;
        align-items: center;
        overflow: hidden;
    }
`